{% extends 'base.html.twig' %}


{# @var location \App\Entity\Location #}
{# @var weather \App\Entity\WeatherData #}

{% block title %}Weather in {{ location.name }}, {{ location.country }}{% endblock %}

{% block body %}
    <main>
        <h1>Weather in {{ location.name }}, {{ location.country }}</h1>

        <ul>
            {% for measurement in measurements %}
                <li>{{ measurement.getMeasurementTime|date('Y-m-d H:i:s')}}: {{ measurement.getTempereature }}&deg;C</li>
            {% endfor %}
        </ul>
    </main>
{% endblock %}
